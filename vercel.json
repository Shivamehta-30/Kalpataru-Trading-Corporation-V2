{
    "builds": [{
        "src": "contract_form/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "50mb", "runtime": "python3.10" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "contract_form/wsgi.py"
        }
    ]
}